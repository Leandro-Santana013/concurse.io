# Known layouts

This directory records fixtures already present in the repository or local
environment whose legacy output is known enough to use as a regression
baseline. The PDF files are referenced by golden/corpus.json; they are not
copied into this directory.
